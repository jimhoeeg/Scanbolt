import { GarageDashboard } from '@/components/garage/GarageDashboard';

/**
 * MODULE 2 — /garage route. Track 1 entry point for both B2C owners and
 * dealer fleet managers; the dashboard adapts itself to the role.
 */
export default function GaragePage() {
  return <GarageDashboard />;
}
